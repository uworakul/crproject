BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[sys_period] ADD [IsCurrent] BIT NOT NULL CONSTRAINT [sys_period_IsCurrent_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
